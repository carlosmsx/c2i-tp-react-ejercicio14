import React, {useEffect, useState} from 'react';

const API_URL = process.env.REACT_APP_API_URL;

console.log(API_URL);

const Home = () => {
    return (
        <div>
            <h2>Home</h2>
        </div>
    );
};

export default Home;